---
title: "Markdownの使い方練習"
author: "Daisuke Takada"
date: "2026-02-25"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# まずはRとRStudioをインストールして、使ってみよう。

- この.Rmdファイルを使って、**Markdownで課題を提出する**という練習をしてみましょう。（あえて超シンプルなMarkdownにしています）

- 自分なりにこのファイルの表現を書き換えて、**授業前の自分にこう説明したらわかりやすいかな？**という資料を完成させて、提出してください。（いずれはGithubのpushを使おうと思いますが、まずはSlackで提出してください）

## とりあえず計算してみよう（電卓のような使い方）

RStudio画面の左下の「Console」に次のものをいれてみよう。

```{r}
1 + 1
```

```{r}
2 - 1
```

### 課題１

-   Markdown形式で、説明文をつけながら掛け算と割り算をやってみよう

Markdownの中で解析ソフトに計算してもらうために**コードチャンク (`{r}`) を挿入** するには、Windowsでは( `Ctrl` + `Alt` + `I` )、Macでは( `Cmd` + `Option` + `I`)を押すと、コードチャンクが挿入されます。

-   $(5+4) \div 3 - 2 \times 1$を計算するにはどう表現するか？

数式はTeXと言って、Markdownの中で`$`で囲むと数式として表現できます。

-   ついでに、Markdownの`##`の数を変えて、見出しの大きさを変えてみましょう

生成AIに指示を出す時に、`#`をつけてから書くとAIが理解しやすいと言われていますが、Markdownの中で`#`は見出しを表すために使われます。`#`の数が多いほど見出しの大きさが小さく(階層が下に)なります。

## 次に、これは何が起こっているだろうか？

```{r}
x <- 1
x + 1
```

```{r}
y <- 2
y - 1
```

ここで、右上のEnvironmentタブを見てみよう。xとyができているはず。

文字も同様に+で足してみると、数字ではないためエラーがでる。ではどうするか？
(下に答えは書いてあるけど、一度自分でやってみましょう)

```{r}
a <- "not"
b <- "elegant"
# Rではスクリプトの中の#の行は何を書いても良いので、メモに使う(コメントアウトといって、無視されます)
paste(a, b)
```

## 数字ではないため、ってどういうこと？

データの中身を確認する

```{r}
str(x)
str(a)
```

イメージ的には、numは「数」、chrは「文字」という意味で、Rはデータの中身を見て、数なのか文字なのかを判断していると考えるとわかりやすいかもしれません。<br>

（ちなみに、intは整数、dblは小数点を含む数、logiはTRUE/FALSEを表す論理値と言います。）


## 数字の塊も扱える(ベクトルと表現されます)

```{r}
z <- 1:10
z
str(z)
```

図にもできる

```{r}
plot(z, 2 * z, type = "b")
```

## 課題２

先生のBMIを計算してみましょう。

体重：[*Markdownで書き換える*]<br> 身長：[*Markdownで書き換える*]

として

$$BMI = \frac{体重}{身長^2}$$

# eGFRを計算する式を調べて作って、計算してみましょう

まずは何の値が必要か調べてみましょう。

クレアチニン：[*Markdownで書き換える*]<br> 年齢：[*Markdownで書き換える*]<br> 性別：[*Markdownで書き換える*]

---

## 課題３（便利なショートカットキーの一部）

多分機能を読んでもわかんないだろうから、全部自分で試してみて、何が起こるかを見てみよう。

| 機能 | Windows / Linux | macOS |
|:-----------------------|:-----------------------|:-----------------------|
| **現在の行を実行** | `Ctrl` + `Enter` | `Cmd` + `Enter` |
| **コメントアウト（解除）** | `Ctrl` + `Shift` + `C` | `Cmd` + `Shift` + `C` |
| **コードチャンク (`{r}`) を挿入** | `Ctrl` + `Alt` + `I` | `Cmd` + `Option` + `I` |
| **全ファイルから検索** | `Ctrl` + `Shift` + `F` | `Cmd` + `Shift` + `F` |
| **コードの自動整形** | `Ctrl` + `Shift` + `A` | `Cmd` + `Shift` + `A` |
| **ショートカット一覧を表示** | `Alt` + `Shift` + `K` | `Option` + `Shift` + `K` |
| **変数名の一括置換** | `Ctrl` + `Alt` + `Shift` + `M` | `Ctrl` + `Option` + `Shift` + `M` |
| **チャンク全体を実行** | `Ctrl` + `Shift` + `Enter` | `Cmd` + `Shift` + `Enter` |

